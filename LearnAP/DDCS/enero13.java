package LearnAP.DDCS;
import java.util.Scanner;

public class enero13 {
    public static void main(String[] args) {
        // Scanner input = new Scanner(System.in);
        // String phoneNum = input.nextLine();
        // System.out.println(phoneNum.substring(0,3)+"****"+phoneNum.substring(7));
        int randList[] = new int[100];
        for (int i = 0;i<100;i++) { //inserting random numbers
            randList[i] = (int)(Math.random()*1000);
        }
        for (int i = 0;i<100;i++) {
            for (int k = 0;i<100;i++) {
                
            }
        }
    }
}
